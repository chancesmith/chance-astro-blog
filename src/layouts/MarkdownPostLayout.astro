---
import BaseLayout from "./BaseLayout.astro";
const { frontmatter } = Astro.props;
---

<BaseLayout pageTitle={frontmatter.title}>
  <p>by Chance Smith</p>
  <!-- {frontmatter.image.url ? <img src={frontmatter.image.url} width="300" alt={frontmatter.image.alt} /> : null} -->
  <slot />
</BaseLayout>
